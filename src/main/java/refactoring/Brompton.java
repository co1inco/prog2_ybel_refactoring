package refactoring;

public class Brompton extends Bike {

    public Brompton(
        String productName,
        double price,
        int maxSpeed,
        int rearGearCount,
        int frontGearCount) {

        super(productName, price, maxSpeed, rearGearCount, frontGearCount);
    }
}
